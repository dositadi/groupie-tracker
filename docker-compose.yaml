services:
  app: 
    build: .
    expose:
      - 8081
    ports:
      - 8081:8080
    environment:
      - OpenCageApiKey=${OpenCageApiKey}
    volumes:
      - .:/groupie-tracker
      - /groupie-tracker/tmp
