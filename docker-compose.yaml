services:
  app: 
    build: .
    expose:
      - 8080
    ports:
      - 8080:8080
    environment:
      - OpenCageApiKey=${OpenCageApiKey}
    volumes:
      - .:/groupie-tracker
      - /groupie-tracker/tmp
