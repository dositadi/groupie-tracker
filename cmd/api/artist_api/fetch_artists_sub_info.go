package artistapi

func (a *ArtistInfo) FetchArtistsSubInfo(artists *[]Artist) *[]Artist {
	if artists == nil {
		return nil
	}

	/* temp := *artists

	defer func() {
		clear(temp)
	}()

	for _, artist := range temp {

	} */
	return nil
}
